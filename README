# 🚀 Full-Stack Project (Frontend + Backend)

A production-ready full-stack project containing:

- 🎯 **Frontend — Next.js 15 (React 19, Tailwind CSS)**
- ⚙️ **Backend — Node.js + TypeScript (compiled to `dist/`)**

---

## 🧾 Tech Stack

- **Frontend:** Next.js • React • Tailwind CSS  
- **Backend:** Node.js • TypeScript • Express  
- **Build Tools:** tsc • nodemon  
- **Package Managers:** npm

---

## 📁 Project Structure

```
root/
 ├── frontend/   # Next.js 15 (React 19, Tailwind)
 └── backend/    # Node.js + TypeScript (compiled to dist/)
```

---

## ⚡ Getting Started

### ▶️ Run Frontend (Dev Mode)

```bash
cd frontend
npm install
npm run dev
```

---

## 🧩 Backend — Scripts

Path: `backend/package.json`

| Command | Description |
|--------|-----------|
| `npm run dev` | Watch + auto-restart (tsc + nodemon) |
| `npm run build` | Compile TypeScript to `dist/` |
| `npm run start` | Run compiled server |

Run backend in dev mode:

```bash
cd backend
npm install
npm run dev
```

Build backend for production:

```bash
npm run build
npm run start
```

---

## 🏗️ Production Build (Frontend)

```bash
cd frontend
npm run build
npm run start
```

---

## 📌 Folder Naming Recommendation

```
frontend/   → client UI
backend/    → API server
```

Keep separate:

- `.env`
- `node_modules`
- build outputs

---

## 📝 Best Practices

✔ Add `.gitignore` in both projects  
✔ Do NOT push `node_modules`  
✔ Commit build output **only if** deployment requires it

---

## 🤝 Contributing

Contributions, suggestions, and improvements are welcome!

---

## ⭐ Support

If this project helped you — consider giving it a ⭐🙂
